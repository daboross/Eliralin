From d27c16ee454ee0132b9d07b67b127df894eee10e Mon Sep 17 00:00:00 2001
From: Dabo Ross <daboross@daboross.net>
Date: Mon, 13 Jan 2014 18:58:30 -0800
Subject: [PATCH] Change reply format from `(<nick>) <message>` to `<munged
 nick>, <message>`


diff --git a/core/main.py b/core/main.py
index 0054b0a..55f3231 100644
--- a/core/main.py
+++ b/core/main.py
@@ -1,5 +1,6 @@
 import thread
 import traceback
+from util import text
 
 
 thread.stack_size(1024 * 512)  # reduce vm size
@@ -12,6 +13,7 @@ class Input(dict):
         chan = paraml[0].lower()
         if chan == conn.nick.lower():  # is a PM
             chan = nick
+        mnick = text.munge(nick)
 
         def message(message, target=chan):
             """sends a message to a specific or current channel/user"""
@@ -22,7 +24,7 @@ class Input(dict):
             if target == nick:
                 conn.msg(target, message)
             else:
-                conn.msg(target, u"({}) {}".format(nick, message))
+                conn.msg(target, u"{}, {}".format(mnick, message))
 
         def action(message, target=chan):
             """sends an action to the current channel/user or a specific channel/user"""
-- 
1.8.3.2

