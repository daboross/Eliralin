From 34118d6f3692ad5353fbab88e9653b3f602f8a29 Mon Sep 17 00:00:00 2001
From: Dabo Ross <daboross@daboross.net>
Date: Sat, 26 Apr 2014 18:39:33 -0700
Subject: [PATCH] Change reply format from ({}) to {},


diff --git a/cloudbot/core/events.py b/cloudbot/core/events.py
index 53af157..4b48ea6 100644
--- a/cloudbot/core/events.py
+++ b/cloudbot/core/events.py
@@ -126,7 +126,7 @@ class BaseEvent:
         if target == self.nick:
             self.conn.msg(target, message)
         else:
-            self.conn.msg(target, "({}) {}".format(self.nick, message))
+            self.conn.msg(target, "{}, {}".format(self.nick, message))
 
     def action(self, message, target=None):
         """sends an action to the current channel/user or a specific channel/user
@@ -247,4 +247,4 @@ class RegexEvent(BaseEvent):
         super().__init__(bot=bot, conn=conn, hook=hook, base_event=base_event, irc_raw=irc_raw, irc_prefix=irc_prefix,
                          irc_command=irc_command, irc_paramlist=irc_paramlist, irc_message=irc_message, nick=nick,
                          user=user, host=host, mask=mask)
-        self.match = match
\ No newline at end of file
+        self.match = match
-- 
1.9.3

